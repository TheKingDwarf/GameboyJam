
hAxis = keyboard_check(vk_right) - keyboard_check(vk_left);
vAxis = keyboard_check(vk_down) - keyboard_check(vk_up);
jumpReleased = keyboard_check_released(vk_up);
	
but1 = keyboard_check(ord("X"));
but2 = keyboard_check(ord("C"));
but3 = keyboard_check(ord("V"));
